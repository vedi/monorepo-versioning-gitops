apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patches:
- path: deployment.yaml

commonLabels:
  app: api-dev

nameSuffix: -dev

images:
- name: api
  newName: ghcr.io/vedi/monorepo-demo/api
  newTag: dev-228a42fd92c33076f7285ebee70afde11eff151f

resources:
- ../../base
